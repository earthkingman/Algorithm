#include <stdio.h>
#include <vector>
using namespace std;

int main(void)
{
    int n, count, j;

    scanf("%d", n);
    vector <int> a(n);
    for (int i = 2; i <= n; i++)
    {
        j = 2;
        while (1)
        {
            if (i % 2 == 0)
            {

            }
        }
    }
    return (0);
}
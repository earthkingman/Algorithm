#include <stdio.h>

int main(void)
{
    int n, m;
    int max_time;
    int count;

    max_time = 0;
    count = 0;
    scanf("%d", &n);
    for (int i = 0; i < n; i++)
    {
        scanf("%d", &m);
        if ()
    }
    return (0);
}